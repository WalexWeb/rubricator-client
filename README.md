<div align="center">
  <h1>📋 Рубрикатор обращений</h1>
  <p>
    <strong>Веб-приложение для автоматической классификации обращений по рубрикам с использованием машинного обучения</strong>
  </p>
  <p>
    <a href="#-технологии">Технологии</a> •
    <a href="#-структура-проекта">Структура</a> •
    <a href="#-возможности">Возможности</a> •
    <a href="#-установка-и-запуск">Установка</a>
  </p>
</div>

---

## 📝 Описание

Рубрикатор обращений — это современное веб-приложение для автоматической классификации текстовых обращений по 20 предопределенным рубрикам. Приложение использует гибридный подход машинного обучения, сочетающий семантический анализ и обработку ключевых слов для обеспечения высокой точности классификации.

### Основные характеристики

- ✅ Автоматическая классификация текстовых обращений
- ✅ Поддержка загрузки файлов (PDF, DOC, DOCX, RTF, TXT)
- ✅ Пакетная обработка нескольких файлов
- ✅ Отображение уверенности модели для каждой рубрики
- ✅ Интерактивный интерфейс с анимациями
- ✅ Адаптивный дизайн для мобильных устройств

---

## 🛠️ Технологии

### Основной стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| **React** | ^19.2.0 | UI библиотека для создания интерфейсов |
| **TypeScript** | ~5.9.3 | Типизированный JavaScript для надежности кода |
| **Vite** | ^7.2.4 | Быстрый сборщик и dev-сервер |
| **Tailwind CSS** | ^4.1.18 | Utility-first CSS фреймворк |

### UI и анимации

| Библиотека | Версия | Описание |
|------------|--------|----------|
| **Framer Motion** | ^12.26.2 | Библиотека анимаций для React |
| **Lucide React** | ^0.562.0 | Набор иконок в SVG формате |

### Работа с данными

| Библиотека | Версия | Описание |
|------------|--------|----------|
| **Axios** | ^1.13.2 | HTTP клиент для запросов к API |

### Инструменты разработки

| Инструмент | Версия | Описание |
|------------|--------|----------|
| **ESLint** | ^9.39.1 | Линтер для проверки кода |
| **Prettier** | ^3.7.4 | Форматтер кода |
| **TypeScript ESLint** | ^8.46.4 | ESLint правила для TypeScript |

---

## 📁 Структура проекта

```
src/
├── components/              # React компоненты
│   ├── chat/               # Компоненты чата и сообщений
│   │   ├── AdditionalRubric.tsx       # Дополнительные варианты рубрик
│   │   ├── ChatContainer.tsx          # Контейнер чата
│   │   ├── FileResultCard.tsx         # Карточка результата файла
│   │   ├── FileResultsContent.tsx     # Контент результатов файлов
│   │   ├── InputArea.tsx              # Область ввода сообщений
│   │   ├── MessageBubble.tsx          # Пузырек сообщения
│   │   ├── MessageList.tsx            # Список сообщений
│   │   └── RubricContent.tsx          # Контент рубрики
│   ├── sidebars/          # Боковые панели
│   │   ├── LeftSidebar.tsx            # Левая панель (статистика)
│   │   └── RightSidebar.tsx           # Правая панель (информация)
│   ├── ui/                # Переиспользуемые UI компоненты
│   │   └── LoadingSpinner.tsx         # Индикатор загрузки
│   └── WelcomeScreen.tsx  # Экран приветствия
├── hooks/                 # Пользовательские React хуки
│   ├── useChat.ts         # Хук для управления чатом и API запросами
│   └── useClipboard.ts    # Хук для работы с буфером обмена
├── types/                 # TypeScript типы и интерфейсы
│   └── chat.ts            # Типы для чата и рубрик
├── utils/                 # Вспомогательные функции
│   └── formatters.ts      # Функции форматирования текста
├── App.tsx                # Главный компонент приложения
├── main.tsx               # Точка входа в приложение
└── index.css              # Глобальные стили
```

---

## ✨ Возможности

### 📝 Классификация текста

- Ввод текста обращения напрямую в интерфейсе
- Мгновенный анализ и получение наиболее подходящей рубрики
- Отображение уверенности модели в процентах
- Показ альтернативных вариантов классификации

### 📎 Работа с файлами

- Загрузка одного файла с детальным результатом
- Пакетная обработка нескольких файлов
- Поддерживаемые форматы: PDF, DOC, DOCX, RTF, TXT
- Отображение статистики успешных и неудачных обработок

### 🎨 Интерфейс

- Современный и интуитивный дизайн
- Плавные анимации при взаимодействии
- Адаптивная верстка для всех устройств
- Темная/светлая тема (настраивается через Tailwind)

### 📊 Дополнительные функции

- Статистика запросов и обработанных файлов
- Копирование результатов в буфер обмена
- Развертывание/сворачивание дополнительных рубрик
- Показ/скрытие шаблонов ответов

---

## 🚀 Установка и запуск

### Требования

- Node.js >= 18.0.0
- npm, yarn или bun

### Локальная разработка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd rubricator-client
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   # или
   yarn install
   # или
   bun install
   ```

3. **Создайте файл `.env` в корне проекта:**
   ```env
   VITE_API_URL=http://localhost:8800
   ```

4. **Запустите dev-сервер:**
   ```bash
   npm run dev
   # или
   yarn dev
   # или
   bun dev
   ```

5. **Откройте браузер:**
   Приложение будет доступно по адресу [http://localhost:5173](http://localhost:5173)

### Сборка для продакшена

```bash
npm run build
# или
yarn build
# или
bun build
```

Собранные файлы будут находиться в папке `dist/`.

### Предпросмотр продакшен сборки

```bash
npm run preview
# или
yarn preview
# или
bun preview
```

---

## 🐳 Docker

### Сборка образа

```bash
docker build -t rubricator-client .
```

### Запуск контейнера

```bash
docker run -d -p 3800:3800 \
  -e VITE_API_URL=http://your-backend:8800 \
  rubricator-client
```

Или при сборке:

```bash
docker build --build-arg VITE_API_URL=http://your-backend:8800 \
  -t rubricator-client .
docker run -d -p 3800:3800 rubricator-client
```

---


## 🏗️ Архитектура

### Гибридный подход ML-модели

Приложение взаимодействует с бэкенд API, который использует:

- **Sentence Transformers** (paraphrase-multilingual-mpnet-base-v2) для семантического анализа
- **Анализ ключевых слов** для уточнения категории
- **Взвешенная комбинация** методов для максимальной точности

### Клиентская архитектура

- **React 19** с функциональными компонентами и хуками
- **TypeScript** для типобезопасности
- **Модульная структура** с разделением на компоненты, хуки, утилиты и типы
- **Custom hooks** для инкапсуляции бизнес-логики
